"""Reusable application logic for ContextPack Desktop.

The legacy ``contextpack_gui.py`` entry point remains in place so existing
shortcuts and the installer continue to work while logic moves into modules.
"""

