# Contributing

Issues and pull requests are welcome. Keep generated documents, personal data, `.venv`, and OCR model binaries out of commits. Test PowerShell syntax and the sample CSV conversion before opening a pull request.

```powershell
.\setup.ps1
.\check-environment.ps1
.\convert-to-markdown.ps1 ".\examples\sample.csv"
```
